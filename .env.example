# -----------------------------------------------------------------------------
# NEXTCRAFT ENVIRONMENT CONFIGURATION (TEMPLATE)
# -----------------------------------------------------------------------------

# [DATABASE CONFIGURATION]
# Individual database credentials for MySQL
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_PORT=3306
DB_NAME=nextcraft

# [AUTHENTICATION]
# Secure key for JWT session encryption.
JWT_SECRET="replace_with_a_long_random_string"

# [APP SETTINGS]
# The primary URL of your application.
NEXT_PUBLIC_APP_URL="http://localhost:3000"
